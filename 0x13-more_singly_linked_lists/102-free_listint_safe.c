#include <stlib.h>
#include "lists.h"
/**
 * free_listint_safe - frees a listint_t list
 * @h: a pointer that points to a pointer
 * Return: size of list freed
 */
size_t free_listint_safe(listint-t **h)
{
	size_t size = 0;
	return (size);
}
